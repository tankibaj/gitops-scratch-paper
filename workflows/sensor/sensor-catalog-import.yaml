apiVersion: argoproj.io/v1alpha1
kind: Sensor
metadata:
  name: sensor-catalog-import
spec:
  template:
    serviceAccountName: workflow-generic-sa
  dependencies:
    - name: events
      eventSourceName: eventsource-webhook # This is the name of the event source
      eventName: catalog-import # This is the name of the event in the event source
      filters:
        data:
          - path: "body.ref"
            type: "string"
            value:
              - "refs/heads/main"
              - "refs/heads/stage"
          - path: "body.commits[].modified"
            type: "string"
            value:
              - "drugInfo.json"
          - path: "body.commits[].added"
            type: "string"
            value:
              - "drugInfo.json"

  triggers:
    - template:
        name: webhook-workflow-trigger
        k8s:
          operation: create
          source:
            resource:
              apiVersion: argoproj.io/v1alpha1
              kind: Workflow
              metadata:
                generateName: drug-info-
              spec:
                serviceAccountName: workflow-generic-sa
                entrypoint: main
                arguments:
                  parameters:
                    - name: message
                      value: hello world # the value will get overridden by events dependency
                templates:
                  - name: main
                    inputs:
                      parameters:
                        - name: message
                    container:
                      image: thenaim/alpine-whale:latest
                      command: [ "sh", "-c", "echo $MESSAGE | jq -r ." ]
                      env:
                        - name: MESSAGE
                          value: "{{inputs.parameters.message}}"
          parameters:
            - src:
                dependencyName: events
                dataKey: body
              dest: spec.arguments.parameters.0.value