apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argo-events-runners
  namespace: argocd
  labels:
    name: argo-events
spec:
  project: gitops
  source:
    repoURL: https://github.com/tankibaj/gitops-scratch-paper.git
    targetRevision: main
    path: playground/argo-events-runners
  destination:
    server: https://kubernetes.default.svc
    namespace: argo-events-runners
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true